<div>
  <div class="col1">
	<h1>⭐ 🌟 {{className}} 🌟 ⭐ </h1>
	<p>fra {{school}}</p>

  <div id="count">0</div> 
  <p>mattestykker!</p>
  </div>
  <div class="col2">
  <p class="instruksjon">Kode for tilgang: <b>{{classId}}</b></p>
  <p class="instruksjon">
    Alle som er inne: <ol id="students"><li>(laster inn)</li></ol>
  </p>
</div>
</div>
<script src="/socket.io/socket.io.js"></script>
<script>
  var socket = io('{{{socketConnectURL}}}');
  socket.on('state', function(payload) {
    console.log(payload);
    payload = JSON.parse(payload);
    if (payload.data.totals) {
      document.getElementById('count').firstChild.data = payload.data.totals;
    }
    if (payload.data.students) {
      document.getElementById('students').innerHTML = '<li>' + payload.data.students
      .map(student => student.name)
      .join('</li><li>');
    }
  });
</script>

<form method="post" action="/api/quit">
  <button type="submit">x</button>
</form>